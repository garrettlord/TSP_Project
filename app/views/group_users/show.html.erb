<<<<<<< HEAD
<% provide(:title, "#{Group.find(@group_user.group_id).name}: #{User.find(@group_user.user_id).name}") %>
User: <%= User.find(@group_user.user_id).name %><br>
Group: <%= Group.find(@group_user.group_id).name %>
=======
<%- model_class = GroupUser -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:group_id) %>:</strong></dt>
  <dd><%= link_to Group.find(@group_user.group_id).name, group_path(Group.find(@group_user.group_id)) %></dd>
  <dt><strong><%= model_class.human_attribute_name(:user_id) %>:</strong></dt>
  <dd><%= link_to User.find(@group_user.user_id).name, user_path(User.find(@group_user.user_id)) %></dd>
</dl>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              group_users_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_group_user_path(@group_user), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              group_user_path(@group_user),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
>>>>>>> e720476d02c7fca50f96f5a0209f871bea3ed1a7
